#include<stdio.h>
int main()
{
	int num, i,sum=0;
	printf("enter a number:");
	scanf("%d",&num);
	printf("sum of the digit %d =",num);
	for (;num>0;num=num/10)
	{
		sum=sum+(num%10);
		printf("%d+",num%10);
	}
    printf("\b=%d");
}
